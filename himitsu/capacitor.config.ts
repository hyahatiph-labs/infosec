import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.hiahatf.himitsu',
  appName: 'himitsu',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
